# project/

Used by the `sync_advanced` task as the source directory. Syncs to `/opt/project` on the remote host with advanced ignore patterns (e.g. `.git`, `node_modules`, `*.log`, `.env*`).

Add your own files here to test directory sync.
